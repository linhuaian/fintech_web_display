import Table from './Table';
import tableReducer from '../TablePage/tableSlice'
import {
  setColumns,
  setRows,
} from '../TablePage/tableSlice';

export {
  tableReducer,
  setColumns,
  setRows
}
export default Table;